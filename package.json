{
  "scripts": {
    "vercel-build": "python manage.py collectstatic --noinput --clear && python manage.py migrate --noinput",
    "build": "python manage.py collectstatic --noinput --clear && python manage.py migrate --noinput"
  },
  "dependencies": {
    "tailwindcss": "^3.4.19"
  }
}
